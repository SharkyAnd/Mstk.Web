@*<div data-bind="foreach: reportTypes" style="margin:0 auto">
   
    <div class="row" style="margin-bottom:10px">
        <h5 class="col-md-10" data-bind="text: Name" ></h5>
        <div>
            <button class="btn btn-success" data-bind="click:$parent.createReport.bind($parent,$data)" value="Выгрузить">Выгрузить</button>
        </div>
    </div>
</div>*@
<div class="container-fluid">
    <table data-bind="foreach: reportTypes" style="margin:0 auto;width:100%">
        <colgroup>
            <col width="32%" />
            <col width="32%" />
            <col width="32%" />
            <col width="4%" />
        </colgroup>
        <tr>
            <td data-bind="text: Name" style="height:50px"></td>
            <td style="height:50px">
                <button class="btn btn-success" data-bind="click:$parent.createReport.bind($parent,$data)" value="Выгрузить">Выгрузить</button>
            </td>
            <td></td>
            <td></td>
        </tr>
    </table>
</div>
